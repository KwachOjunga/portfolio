This repo contains a port of Nextjs web app
with static files. 

The theme toggling feature is a bit tricky to set up.

Yeah, i should probably detail it. But briefly,
it heavily relies on the output.css file in styles dir.
The fonts also require you to set them up using the css file.

--- Take care!
